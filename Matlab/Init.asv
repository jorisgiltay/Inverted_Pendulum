close all
clear all
clc

sim('init.slx');


motor_r = 1;
motor_l = 0.6;
motor_k = 0.002;
motor_j = 10;
motor_b = 0.002;

experiment_data = importdata('experiment_giltay.csv');

time = 0:0.01:4;

rpm = experiment_data(:,2);

x01 = 1:numel(time);
x02 = 1:numel(rpm);

xi2 = linspace(1,numel(x02),numel(x01));


inr